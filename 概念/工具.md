
模型输出结构化的调用指令，系统执行后把结果返回给模型。


工具和推理的关系，工具要获取信息，逻辑放在模型推理。
然而，工具有时候也可以有逻辑，什么样的逻辑应该放在工具里面，什么样的逻辑应该交给模型自己呢？
## 工具定义

1. 原子化，最好能够复用，
2. 格式要明确


## 工具调用链

单次调用不是难点，如果是链条，如何处理中间的失败问题


## 常见工具设计的例子

读取类、写入类、执行类


## PM问题

1. 权限分级，不同的操作授权方式应该不同
2. 失败处理，不同场景下不一样，产品应该有默认策略
3. 透明度，用户要知道调用吗？
4. 